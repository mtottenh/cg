[package]
name = "portal-core"
description = "Core types, IDs, and errors for the Gaming Portal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
validator = { workspace = true }
# ToSchema derives only. utoipa is not an async dependency, so this does not
# violate the "core has no async deps" rule in CLAUDE.md. Deriving here rather
# than mirroring the enums in portal-api keeps ONE definition of the wire values
# -- the duplication is what let the frontend drift (P-31).
utoipa = { workspace = true }

[lints]
workspace = true
